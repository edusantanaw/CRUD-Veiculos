describe("Test", () => {
  test("Should return 2", () => {
    const response = 1 + 1;
    expect(response).toBe(2);
  });

  test("Should return 3", ()=> {
    const response = 1+2;
    expect(response).toBe(3)
  })
});
